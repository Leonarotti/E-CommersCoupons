import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CouponsClientFrontend-Ionic',
  webDir: 'www'
};

export default config;
